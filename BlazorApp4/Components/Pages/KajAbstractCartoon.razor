@page "/kajabstractcartoon"
@rendermode InteractiveServer

@inject NavigationManager Navigation

<button @onclick="ToggleWallpaper" style="position:absolute;top:12px;right:12px;z-index:30;">
    Wissel wallpaper
</button>

<div class="iphone18pro-wallpaper">
    <div style="position:absolute;left:0;top:0;width:100%;height:100%;z-index:1;pointer-events:none;">
        <!-- Abstracte achtergrond met SVG -->
        <svg viewBox="0 0 430 932" width="100%" height="100%" style="display:block;width:100%;height:100%;object-fit:cover;">
            <!-- Abstracte vormen -->
            <rect x="0" y="0" width="430" height="932" fill="#e0f7fa" />
            <ellipse cx="120" cy="200" rx="90" ry="60" fill="#b3e6ff" opacity="0.7" />
            <ellipse cx="350" cy="400" rx="110" ry="80" fill="#42B16D" opacity="0.18" />
            <ellipse cx="215" cy="800" rx="180" ry="60" fill="#42B16D" opacity="0.12" />
            <circle cx="370" cy="120" r="60" fill="#3a6fa8" opacity="0.18" />
            <!-- Kaj cartoon -->
            <!-- Lichaam -->
            <ellipse cx="215" cy="700" rx="38" ry="60" fill="#3a6fa8" />
            <!-- Hoofd -->
            <circle cx="215" cy="630" r="32" fill="#ffe0b3" stroke="#333" stroke-width="2" />
            <!-- Pet -->
            <ellipse cx="215" cy="610" rx="28" ry="12" fill="#42B16D" />
            <rect x="200" y="600" width="30" height="10" rx="5" fill="#245c2a" />
            <!-- Ogen -->
            <ellipse cx="205" cy="635" rx="4" ry="6" fill="#222" />
            <ellipse cx="225" cy="635" rx="4" ry="6" fill="#222" />
            <!-- Mond -->
            <path d="M208 648 Q215 655 222 648" stroke="#b36f3c" stroke-width="2" fill="none" />
            <!-- Sjaal -->
            <rect x="195" y="655" width="40" height="12" rx="6" fill="#e23d3d" />
            <rect x="230" y="667" width="10" height="22" rx="5" fill="#e23d3d" />
            <!-- Armen -->
            <rect x="170" y="700" width="20" height="8" rx="4" fill="#ffe0b3" transform="rotate(-25 180 704)" />
            <rect x="240" y="700" width="20" height="8" rx="4" fill="#ffe0b3" transform="rotate(25 250 704)" />
            <!-- Benen -->
            <rect x="200" y="760" width="10" height="40" rx="5" fill="#245c2a" transform="rotate(-10 205 780)" />
            <rect x="220" y="760" width="10" height="40" rx="5" fill="#245c2a" transform="rotate(10 225 780)" />
            <!-- Schoenen -->
            <rect x="198" y="797" width="14" height="6" rx="3" fill="#888" />
            <rect x="218" y="797" width="14" height="6" rx="3" fill="#888" />
        </svg>
    </div>
    <div class="iphone18pro-content">
        <h1 class="gambamark-title premium">Kaj (Cartoon, Abstract)</h1>
    </div>
</div>

@code {
    string[] WallpaperRoutes = new[]
    {
        "/forestwallpaper",
        "/saunawallpaper",
        "/kajwallpaper",
        "/kajspotlightwallpaper",
        "/kaionicecartoon",
        "/kajabstractcartoon",
        "/home",
        "/weather"
    };

    void ToggleWallpaper()
    {
        var current = Navigation.Uri.ToLower();
        var baseUri = Navigation.BaseUri.ToLower();
        var relative = current.StartsWith(baseUri) ? current.Substring(baseUri.Length - 1) : current;
        int idx = Array.FindIndex(WallpaperRoutes, r => relative.StartsWith(r));
        int nextIdx = (idx + 1) % WallpaperRoutes.Length;
        Navigation.NavigateTo(WallpaperRoutes[nextIdx]);
    }
}

<style>
.iphone18pro-wallpaper {
    width: 430px;
    height: 932px;
    border: 8px solid #222;
    border-radius: 48px;
    box-shadow: 0 0 32px rgba(0,0,0,0.2);
    overflow: hidden;
    position: relative;
    background: none;
    margin: 32px auto 0 auto;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}
.gambamark-title.premium {
    font-family: 'Merriweather', 'Georgia', serif;
    font-size: 2.9rem;
    font-weight: 900;
    letter-spacing: 0.10em;
    color: #42B16D !important;
    margin-bottom: 0.2em;
    margin-top: 2.2em;
    text-align: center;
    z-index: 2;
    position: relative;
    border-bottom: 2px solid #42B16D;
    border-radius: 0 0 12px 12px;
    padding-bottom: 0.15em;
}
.iphone18pro-content {
    position: relative;
    z-index: 2;
    text-align: center;
    margin-top: 3em;
    color: #fff;
    text-shadow: 0 2px 8px #222;
}
</style>
