@page "/f"
@rendermode InteractiveServer

@inject NavigationManager Navigation

<button @onclick="ToggleWallpaper" style="position:absolute;top:12px;right:12px;z-index:30;">
    Wissel wallpaper
</button>

<div class="iphone18pro-wallpaper">
    <div class="dansgolv-title">Dansgolv</div>
    <img src="/dansgolv/dansgolv4.png" alt="Dansgolv Bidden" class="dansgolv-foto-bidden" />
    <div class="dansgolv-fotos">
        <img src="/dansgolv/dansgolv1.png" alt="Dansgolv 1" class="dansgolv-foto left" />
        <img src="/dansgolv/dansgolv2.png" alt="Dansgolv 2" class="dansgolv-foto right" />
    </div>
    <div class="dansgolv-fotos-extra">
        <img src="/dansgolv/dansgolv5.png" alt="Dansgolv 5" class="dansgolv-foto extra" />
        <img src="/dansgolv/dansgolv6.png" alt="Dansgolv 6" class="dansgolv-foto extra" />
    </div>
    <img src="/dansgolv/dansgolv-wallpaper2.png" alt="Dansvloer" style="width:100%;height:100%;object-fit:cover;display:block;" />
</div>
<div class="apple-watch-42-face">
    <svg viewBox="0 0 126 157" width="126" height="157" aria-label="Apple Watch 42mm">
        <defs>
            <clipPath id="watch42ScreenClip">
                <rect x="0" y="0" width="126" height="157" rx="32" />
            </clipPath>
            <filter id="watch42Shadow" x="-20%" y="-20%" width="140%" height="140%">
                <feDropShadow dx="0" dy="6" stdDeviation="8" flood-color="#000" flood-opacity="0.18" />
            </filter>
        </defs>

    </svg>
    <div class="apple-watch-label">Apple Watch 42mm</div>
</div>

@code {
    string[] WallpaperRoutes = new[]
    {
        "/forestwallpaper",
        "/saunawallpaper",
        "/kajwallpaper",
        "/kajspotlightwallpaper",
        "/home",
        "/weather"
    };

    // Persoonlijke watermark tekst
    string PersonalWatermarkText = "LB";

    // Base64-gecodeerde SVG met onzichtbare watermark
    string Base64WatermarkSvg =>
        "data:image/svg+xml;base64," +
        Convert.ToBase64String(System.Text.Encoding.UTF8.GetBytes($@"<svg xmlns='http://www.w3.org/2000/svg' width='430' height='932' viewBox='0 0 430 932'><text x='50%' y='95%' text-anchor='middle' font-size='32' fill='#000' opacity='0.01' font-family='Arial, sans-serif'>{PersonalWatermarkText}</text></svg>"));

    void ToggleWallpaper()
    {
        var current = Navigation.Uri.ToLower();
        var baseUri = Navigation.BaseUri.ToLower();
        var relative = current.StartsWith(baseUri) ? current.Substring(baseUri.Length - 1) : current;
        int idx = Array.FindIndex(WallpaperRoutes, r => relative.StartsWith(r));
        int nextIdx = (idx + 1) % WallpaperRoutes.Length;
        Navigation.NavigateTo(WallpaperRoutes[nextIdx]);
    }
}

<style>


    .dansgolv-fotos-extra .foto7-hoog {
        margin-top: -32px;
    }
    .dansgolv-fotos-extra {
        position: absolute;
        top: 75%;
        left: 0;
        width: 100%;
        display: flex;
        justify-content: space-around;
        pointer-events: none;
        z-index: 11;
    }

    .dansgolv-foto.extra {
        width: 90px;
        height: 90px;
        object-fit: cover;
        border: 4px solid #fff;
        border-radius: 18px;
        box-shadow: 0 4px 24px #0005, 0 0 12px #ff5e6290;
        background: #fff8;
        transform: rotate(-6deg) scale(1.03);
        transition: transform 0.3s;
        pointer-events: auto;
        margin: 0 6px;
    }

        .dansgolv-foto.extra:hover {
            transform: scale(1.13) rotate(0deg);
            z-index: 12;
        }

    .dansgolv-foto-bidden {
        position: absolute;
        top: 70%; /* was 58% */
        left: 50%;
        transform: translate(-50%, -50%) rotate(-7deg);
        width: 120px;
        height: 120px;
        object-fit: cover;
        border-radius: 50%;
        border: 5px solid #fff;
        box-shadow: 0 0 32px 8px #0005, 0 0 12px #ff5e6290;
        background: #fff8;
        z-index: 12;
        pointer-events: auto;
        transition: transform 0.3s, box-shadow 0.3s;
    }
.dansgolv-foto-bidden:hover {
    transform: translate(-50%, -50%) scale(1.12) rotate(0deg);
    box-shadow: 0 0 48px 16px #ffe066ee, 0 8px 32px #0007;
}
    .dansgolv-fotos {
        position: absolute;
        top: 60%;
        left: 0;
        width: 100%;
        display: flex;
        justify-content: space-between;
        pointer-events: none;
        z-index: 11;
    }

    .dansgolv-foto {
        width: 100px;
        height: 100px;
        object-fit: cover;
        border: 4px solid #fff;
        border-radius: 18px;
        box-shadow: 0 4px 24px #0005, 0 0 12px #ff5e6290;
        background: #fff8;
        transform: rotate(-8deg) scale(1.05);
        transition: transform 0.3s;
        pointer-events: auto;
    }

        .dansgolv-foto.right {
            margin-right: 18px;
            transform: rotate(8deg) scale(1.05);
        }

        .dansgolv-foto.left {
            margin-left: 18px;
        }

        .dansgolv-foto:hover {
            transform: scale(1.15) rotate(0deg);
            z-index: 12;
        }
    .dansgolv-title {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 10;
        font-family: 'Merriweather', 'Georgia', serif;
        font-size: 3.2rem;
        font-weight: 900;
        letter-spacing: 0.08em;
        background: linear-gradient(90deg, #ff9966 0%, #ff5e62 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-fill-color: transparent;
        text-shadow: 0 2px 24px #ff5e6290, 0 0 8px #fff, 0 0 2px #ff9966;
        filter: brightness(1.12);
/*         border-bottom: 2px solid #ff5e62;
        border-radius: 0 0 12px 12px; */
        text-align: center;
        width: 90%;
        pointer-events: none;
        user-select: none;
        padding-bottom: 0.15em;
    }
    .kaj-accordion {
        display: inline-block;
        cursor: pointer;
        transition: transform 0.4s cubic-bezier(.68,-0.55,.27,1.55);
        will-change: transform;
    }

        .kaj-accordion:hover {
            transform: scaleY(1.5) scaleX(1.1);
        }
    .fullscreen-bg {
        position: fixed;
        left: 0;
        top: 0;
        width: 100vw;
        height: 100vh;
        z-index: 0;
        pointer-events: none;
    }

    .photo-polaroid.premium {
        position: absolute;
        z-index: 2;
        width: 120px;
        background: linear-gradient(135deg, #f8fafc 80%, #e0e7ef 100%);
        border-radius: 18px;
        box-shadow: 0 12px 36px #0003, 0 2px 12px #fff2 inset;
        padding: 12px 12px 26px 12px;
        display: flex;
        flex-direction: column;
        align-items: center;
        border: 2.5px solid #eaeaea;
        filter: drop-shadow(0 2px 8px #0002);
        transition: none;
        overflow: hidden;
    }

        .photo-polaroid.premium img {
            width: 100%;
            border-radius: 12px;
            box-shadow: 0 2px 8px #0001;
            margin-bottom: 7px;
            border: 1.5px solid #e0e7ef;
            background: #f6f8fa;
        }

    .polaroid-gloss {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 32px;
        border-radius: 18px 18px 0 0;
        background: linear-gradient(180deg,rgba(255,255,255,0.38) 0%,rgba(255,255,255,0.08) 100%);
        pointer-events: none;
    }

    .gambamark-title.premium {
        font-family: 'Merriweather', 'Georgia', serif;
        font-size: 2.9rem;
        font-weight: 900;
        letter-spacing: 0.10em;
        color: #42B16D !important;
        /* background: linear-gradient(90deg, #42B16D); */
        /* -webkit-background-clip: text; */
        /* background-clip: text; */
        /* text-fill-color: transparent; */
        /*    text-shadow: 0 2px 32px #245c2a, 0 0 16px #fff, 0 0 2px ;
        filter: brightness(1.15);
        margin-bottom: 0.2em;
        margin-top: 2.2em;
        text-align: center;
        z-index: 2;
        position: relative;
        border-bottom: 2px solid #42B16D;
        border-radius: 0 0 12px 12px;
        padding-bottom: 0.15em; */
    }

    .iphone18pro-wallpaper {
        width: 430px;
        height: 932px;
        border: 8px solid #222;
        border-radius: 48px;
        box-shadow: 0 0 32px rgba(0,0,0,0.2);
        overflow: hidden;
        position: relative;
        background: none;
        margin: 32px auto 0 auto;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }

    .iphone18pro-content {
        position: relative;
        z-index: 2;
        text-align: center;
        margin-top: 3em;
        color: #fff;
        text-shadow: 0 2px 8px #222;
    }

    /* Onzichtbare watermark styling */
    .invisible-watermark {
        position: absolute;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        z-index: 3;
        pointer-events: none;
    }

    .apple-watch-42-face {
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-bottom: 32px;
        margin-top: 12px;
    }

    .apple-watch-label {
        font-size: 1rem;
        color: #888;
        text-align: center;
        margin-top: 4px;
        font-family: 'Segoe UI', Arial, sans-serif;
        letter-spacing: 0.04em;
    }
</style>
